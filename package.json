{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "start:dev": "pnpm -r --filter ./apps/* start:dev",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.18.3",
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
